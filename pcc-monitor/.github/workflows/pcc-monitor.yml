name: PCC API Update Monitor

on:
  schedule:
    # 每 4 小時跑一次（UTC 時間）
    - cron: '0 */4 * * *'
  workflow_dispatch: # 允許手動觸發

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run monitor
        run: node monitor.mjs --once

      - name: Show current state
        run: |
          echo "=== state.json ==="
          cat state.json
          echo ""
          echo "=== Last 10 log entries ==="
          tail -10 update-log.jsonl

      - name: Run analysis (if enough data)
        run: node analyze.mjs || true

      - name: Commit log updates
        run: |
          git config user.name "PCC Monitor Bot"
          git config user.email "bot@github.actions"
          git add state.json update-log.jsonl
          git diff --staged --quiet || git commit -m "PCC API check: $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
