---
version: "0.1"
updated: "2026-02-18"
status: "草案"
depends: ["00-總論"]
changelog:
  - "0.1 (2026-02-18): 草案建立，從 CLAUDE.md 提煉核心原則"
---

# 02 核心架構

## SSOT（Single Source of Truth）原則

所有常數、狀態值、欄位名稱**只在一個地方定義**，其他地方一律透過 import 使用。

| 資料類型 | 唯一定義位置 |
|----------|-------------|
| 標案狀態值 | `src/lib/constants/bid-status.ts` |
| Notion 欄位名稱 | `src/lib/constants/field-mapping.ts` |
| 功能模組定義 | `src/lib/modules/feature-registry.ts` |
| AI 階段定義 | `src/data/config/stages.ts` |
| 應用程式設定 | `src/lib/settings/types.ts` + `defaults.ts` |

## 模組結構規範

```
src/lib/{feature}/
  types.ts        ← 介面與型別定義（必要）
  index.ts        ← 匯出彙整（選用）
  constants.ts    ← 功能專屬常數（選用）
  helpers.ts      ← 純函式（選用）
  use{Feature}.ts ← React Hook（選用）
  __tests__/      ← 測試檔案（必要）

src/components/{feature}/
  {Component}.tsx  ← UI 元件（只透過 props 接收資料）
```

## 三層分離

```
src/lib/          ← 資料層（商業邏輯、Hook、工具函式、型別）
src/components/   ← 展示層（UI 元件，只負責渲染）
src/app/          ← 頁面層（串接資料層和展示層）
```

## Feature Registry 模式

所有可開關功能在 `FEATURE_REGISTRY` 中註冊，側欄、路由守衛、設定頁面統一讀取。

## 統編優先原則

所有公司識別以統編（8 位數字）為 key，名稱僅供顯示。詳見 ADR-05（03-技術選型）。

## 介面契約

（待補充：各模組之間的輸入/輸出介面定義）

## 閉環原則

閉環有兩層，缺一不可：做對了（正確性）+ 有效（目的性）。
測試全過是做對了，功能真的解決問題才是有效。兩層都通過，閉環才算完成。
詳見 CLAUDE.md §6。
