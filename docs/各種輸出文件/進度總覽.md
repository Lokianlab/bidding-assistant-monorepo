# 進度總覽

最後更新：2026-02-25 session 2（進行中）

---

## 上次結束在哪裡

Session 1（0225）完成架構轉彎 + 第一階段開發手冊（31 項工作項）。

## 當前狀態

**第一階段開發：程式碼完成 90%，測試和 build 驗證進行中**

### 已完成的工作項

#### D. 系統基礎
- [x] D1：Supabase Schema — migration SQL + TS types + 3 client modules
- [~] D2：Cloudflare Tunnel — 跳過，需 Jin 物理操作
- [x] D3：首頁 Dashboard — API route 完成
- [x] D4：角色區分 — auth/roles.ts + types
- [x] D5：決策記錄 — decisions module + API route

#### A. 情報自動拉取
- [x] A1：PCC 情報拉取 — pcc-intel.ts
- [x] A2：觸發機制 — trigger.ts
- [x] A3：RFP 解析 — rfp-parser.ts（Claude CLI + regex fallback）
- [x] A4：RFP 上傳 UI — RFPUpload.tsx
- [x] A5：Perplexity 提示詞 — perplexity-prompts.ts（3 輪假設驗證式）
- [x] A6：勝算五項檢核 — win-assessment.ts + WinAssessmentPanel.tsx
- [x] A7：結果貼回 — PerplexityPanel.tsx + API route
- [x] A8：情報頁整合 — /cases/[id]/intelligence/page.tsx

#### B. 一鍵建案
- [x] B1：觸發協調 — orchestrator.ts
- [x] B2：Notion 建案 — notion-setup.ts
- [x] B3：Drive 資料夾 — drive-setup.ts
- [x] B4：鷹架複製 — copyScaffoldToCase（遞迴複製）
- [x] B5：範本預填 — prefill.ts

#### C. 知識庫卡片索引
- [x] C1：Drive 掃描 — drive-scanner.ts
- [x] C2：文字提取 — text-extractor.ts（PDF/DOCX，掃描 PDF 偵測）
- [x] C3：AI 卡片生成 — card-generator.ts（Claude CLI + fallback）
- [x] C4：Supabase 儲存 — cards-client.ts
- [x] C5：搜尋 UI — /knowledge/page.tsx + SearchBar + KnowledgeCard + CategoryBrowser
- [x] C6：自動推薦 — recommender.ts
- [x] C7：初始化掃描 — initializer.ts + API route
- [x] C8：增量更新 — updater.ts + API route
- [x] C9：設定頁 — /settings/knowledge/page.tsx

#### 額外
- [x] Feature Registry — 4 個新模組已註冊
- [x] TypeScript 型別檢查 — 新增檔案零錯誤

### 已驗證
- [x] 測試：123/123 全過（intelligence helpers 49 + case-setup helpers 29 + win-assessment 45）
- [x] npm run build 成功（所有新頁面和 API route 都在 output）
- [x] TypeScript 零新增錯誤

### 新建檔案統計
約 45 個新檔案：
- 26 個 lib 模組檔
- 11 個 API routes
- 7 個 UI 元件
- 3 個頁面

## 需要 Jin 操作的項目

| # | 項目 | Jin 要做什麼 | 開發時替代方案 |
|---|------|------------|-------------|
| J1 | GOOGLE_SCAFFOLD_FOLDER_ID | Drive 找「BZ. 製標鷹架」folder ID | 用任意 folder ID 測試 |
| J2 | GOOGLE_KB_FOLDER_IDS | 指定要索引哪些 Drive 資料夾 | 用測試資料夾 |
| J3 | Cloudflare Tunnel | 電腦安裝 cloudflared + 域名 | localhost:3000 |
| J4 | Supabase 執行 migration | 在 Dashboard 執行 002-intelligence-mvp.sql | 開發用 mock |

## Jin 指示（0225 s2）

模型省錢策略：批量寫碼用 Sonnet，簡單任務用 Haiku，深度推理才用 Opus。預估省 60-70%。

## 待補項目

- [ ] Perplexity 第三輪：最低標實務報價策略
- [x] ~~測試撰寫~~ 123/123 全過
- [x] ~~npm run build 完整驗證~~ 通過
- [ ] Supabase migration 執行（需 Jin）

## 阻塞中

- 無。

## 文件索引

| 檔案 | 內容 |
|------|------|
| `進度總覽.md` | 當前狀態（本文件） |
| `累積記憶.md` | 跨 session 持久知識 |
| `系統架構指引-幕僚系統.md` | 頂層指引 |
| `第一階段開發手冊.md` | 31 項工作項規格 |
| `開發規範手冊.md` | 編碼規範 |
