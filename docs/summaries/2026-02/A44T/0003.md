# 小結 A44T-0003

日期: 2026-02-20 03:34
機器: A44T
範圍: 自 A44T-0002 至本次
資料來源: context window, commits ed59c54~cf0f942

## A. 議題列表

A1. 修正工具檢查的流程歸屬
  背景：A44T-0002 記錄了「把工具檢查加進 /更新 指令」，但用戶隨後指出這樣不對——/更新 是輕量同步，一次對話中可能跑好幾次，不需要每次都去確認工具有沒有裝。工具不會憑空消失，只有開新 session 時確認一次就好。
  經過：把 /更新 改回原本的四個步驟，工具檢查改放到 CLAUDE.md 的重啟流程 Step 3。另外也發現壓縮前忘了更新快照，補拍到 20260220-1200。
  結論：已完成。重啟時檢查 CLI（gemini、codex）和 MCP（讀 .mcp.json 逐一確認），/更新 不管這件事。A44T-0002 的 A3 和 D1 記的是修正前的版本，本則為更正記錄。

A2. 盤點開發工具是否夠用
  背景：裝完 Gemini CLI 和 Codex CLI 之後，用戶想知道手上有多少工具、還缺什麼。
  經過：列出所有工具——3 個 AI CLI、4 個 MCP server、8 個指令、2 個 hooks、加上 Perplexity 和 Napkin AI 等外部工具。用戶看完之後覺得工具本身夠了，但提出一個觀察：方法論也是重要的工具，而這塊明顯不足。
  結論：工具面齊全，但方法論只有一個能用的（模型委員會），這個缺口需要補。

A3. 窮舉 AI 工作情境，分析方法論覆蓋缺口
  背景：用戶要求先把所有可能的情境列出來再說，不要直覺判斷。
  經過：從「做之前」到「溝通」分成六大類，窮舉出 26 個 AI 在工作中會碰到的情境。逐一檢查現有方法或機制是否覆蓋，發現只有 7 個有某種形式的方法（模型委員會、閉環原則兩層、碰撞處理策略、/更新碰撞偵測、記錄層三層架構、/小結和/待辦）。剩下 19 個完全空白，尤其「做之前」六個幾乎全空，「做的過程」五個也全空。
  現況：已建暫存檔（issue 類，待決）。26 個不用全寫方法論，但哪些要補、補到什麼程度，還沒決定。

A4. 暫存三個討論議題
  背景：這輪討論產出了三個需要記錄的議題，用戶說全部暫存。
  經過：建了兩個新暫存檔（方法論完備性盤點、工具檢查歸屬重啟流程），更新了一個既有暫存檔（CLI 工具生態系補上決策脈絡）。三份都 commit + push 完成。
  結論：三份暫存檔已入庫。

## B. 已定案

B1. 工具檢查只在重啟時做，/更新 不做。理由：/更新 是高頻輕量操作，工具不會憑空消失。
B2. A44T-0002 的 A3 和 D1 記的是修正前的版本（工具檢查放在 /更新），實際已改回重啟流程。本則為更正記錄。

## C. 待決策

**C1. 決定哪些 AI 工作情境需要正式方法論**
背景：26 個情境中 19 個沒有方法覆蓋，但不是每個都需要寫方法論。暫存檔列了幾個方向，用戶還沒表態。
  C1a. 優先補高頻且出過事的——模擬驗證（/kb 出過漏洞）、完備性檢查（/kb 只驗了十分之一）、一致性檢查（暫存檔和實際狀態不一致）
  C1b. 有些靠常識即可（替代路徑、向用戶解釋），不需正式方法
  C1c. 有些是系統問題（決策同步靠 git 就行），不需額外方法論
  C1d. 投入程度未定——是寫成正式文件級方法論，還是在 CLAUDE.md 加幾條規則就好

## D. 檔案變更

D1. `bidding-assistant/docs/dev-plan/_staging/方法論完備性盤點.md` — 新建，26 個情境的覆蓋分析（issue，待決）
D2. `bidding-assistant/docs/dev-plan/_staging/工具檢查歸屬重啟流程.md` — 新建，工具檢查歸屬決策記錄（rule，待推）
D3. `bidding-assistant/docs/dev-plan/_staging/03-AI連接與工具生態系.md` — 更新，補入 CLI 安裝決策脈絡和待辦
D4. `bidding-assistant/docs/dev-plan/_staging/_index.md` — 新增方法論完備性盤點和工具檢查歸屬兩行索引
