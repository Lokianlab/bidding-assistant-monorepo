# 用戶驗證系統

## 已註冊用戶

| 用戶名 | 位階 | PIN 雜湊 (SHA-256 of `{user}:{pin}`) | 註冊日 |
|--------|------|--------------------------------------|--------|
| jin | primary | 27626038be6159ee1c08bbc1ae2cb04480c5c429b62a03583f724ef9886b61f0 | 2026-02-22 |

## 驗證方式

1. 用戶發言時在開頭或結尾附上四位數驗證碼
2. 機器計算 `SHA-256("{用戶名}:{輸入的碼}")` 比對上表雜湊
3. 匹配 → 該用戶身分發言；不匹配或沒碼 → 一般操作

## 位階說明

- **primary**：主用戶（Jin），最高權限，最終裁決權
- **collaborator**：協作者，受授權範圍限制

## 本機設定

每台機器需要一份 `.user-pins`（gitignored），格式：

```
# 用戶名|驗證碼|位階
jin|{四位數}|primary
```

新機器設定時由用戶在本機親自輸入，不透過 git 傳遞明文。

## 管理權限

- Jin 可以修改所有人的驗證碼、刪除用戶
- 其他用戶只能修改自己的驗證碼
- 修改後需更新本檔的雜湊 + 所有本機 `.user-pins`
