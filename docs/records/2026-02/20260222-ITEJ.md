OP|20260222-1720|ITEJ|成功|topic:feat-pricing-refactor
報價計算模組重構：邏輯抽出到 lib/pricing/、26 個測試|F:src/lib/pricing/types.ts,src/lib/pricing/helpers.ts,src/app/tools/pricing/page.tsx
---
背景: 報價計算模組（pricing/page.tsx）所有邏輯都內嵌在頁面元件中，無測試，與品質檢查模組重構前的狀態相同。依照專案架構規範，應該把商業邏輯抽到 lib/ 層。
操作:
1. 建立 src/lib/pricing/types.ts — CostItem、CostCategory、PricingConfig、PricingSummary 型別
2. 建立 src/lib/pricing/helpers.ts — itemAmount、calcCategoryTotals、calculateSummary、formatAmount、createEmptyItem、getDefaultItems 六個純函式
3. 建立 src/lib/pricing/__tests__/helpers.test.ts — 26 個測試覆蓋所有計算邏輯（單筆計算、類別小計、完整摘要含邊界值、工具函式）
4. 重構 page.tsx — 從 255 行內嵌邏輯改為匯入 lib 模組的薄包裝層，移除 CostItem 和 EMPTY_ITEM 定義
結果: 33 檔 681 測試全過（新增 26 個），build 零錯誤。頁面行為完全不變，邏輯可獨立測試。
---
OP|20260222-1730|ITEJ|成功|topic:feat-assembly-refactor
提案組裝引擎重構：純邏輯抽出到 lib/assembly/、31 個測試|F:src/lib/assembly/types.ts,src/lib/assembly/helpers.ts,src/app/assembly/page.tsx
---
背景: 提案組裝引擎（assembly/page.tsx）是產品核心功能，724 行中混雜了純計算邏輯和 React 狀態管理。token 估算、檔案清單計算、檔案名稱生成等純函式應該抽到 lib 層以便獨立測試。
操作:
1. 建立 src/lib/assembly/types.ts — FileListItem 介面
2. 建立 src/lib/assembly/helpers.ts — estimateTokens、formatKB、buildFilename、computeFileList、computeActiveFiles、assembleContent 六個純函式
3. 建立 src/lib/assembly/__tests__/helpers.test.ts — 31 個測試（token 估算 5 + 格式化 4 + 檔名 6 + 檔案清單 6 + 啟用清單 5 + 組裝 4 + L3 撰寫規範驗證）
4. 重構 page.tsx — 刪除內嵌函式，改為 import lib 模組；fileList 和 activeFiles 的 useMemo 改為呼叫 computeFileList / computeActiveFiles；handleAssemble 改用 assembleContent
結果: 35 檔 723 測試全過（+42 新測試），build 零錯誤。頁面行為不變，核心計算邏輯可獨立測試。
---
OP|20260222-1750|ITEJ|成功|topic:feat-quality-refactor
鐵律規則補實作：budgetConsistency、teamConsistency、scopeConsistency 三個 flag|F:src/lib/quality/rules.ts,src/lib/quality/__tests__/rules.test.ts
---
背景: A44T 督導審查（forum feedback ref:a1edfb2）指出 checkIronLaws 的 5 個 flag 中只有 crossValidateNumbers 和 dateConsistency 有實作，其餘 3 個 flag 已在設定頁顯示但沒有效果。選擇實作而非隱藏，因為這些檢查對政府提案有實際價值。
操作:
1. 在 rules.ts 的 checkIronLaws 中實作三個 flag：
   - budgetConsistency：偵測金額/預算/經費/報價/費用等關鍵詞，>3 處觸發提醒
   - teamConsistency：偵測主持人/協同主持人/專任助理/研究員/工程師/顧問/計畫經理，>3 處觸發提醒
   - scopeConsistency：偵測工作範圍/服務範圍/辦理事項/工作項目/服務項目/工作內容，>2 處觸發提醒
2. 在 rules.test.ts 新增 6 個測試（每個 flag 各 2 個：觸發和不觸發的情境）
結果: 35 檔 729 測試全過（品質模組從 42→48 個），build 零錯誤。A44T 回饋的問題已解決。
---
OP|20260222-1910|ITEJ|成功|topic:feat-pricing-refactor
報價 helpers 負數防禦 + 2 個測試|F:src/lib/pricing/helpers.ts,src/lib/pricing/__tests__/helpers.test.ts
---
背景: A44T 督導審查（forum feedback ref:9dcba76）指出 itemAmount 沒有防禦負數輸入。quantity 或 unitPrice 為負數時應視為 0。
操作: 在 helpers.ts 的 itemAmount 函式加入 Math.max(0, ...) 防護 quantity 和 unitPrice，新增 2 個測試驗證負數輸入。
結果: 36 檔 742 測試全過（pricing 26→28），build 零錯誤。
---
OP|20260222-1920|ITEJ|成功|topic:feat-pcc-web
市場趨勢方向判斷功能：judgeTrend 純函式 + UI + 7 個測試|F:src/lib/pcc/types.ts,src/lib/pcc/market-trend.ts,src/components/pcc/MarketTrend.tsx
---
背景: JDNE 的 forum 帖子（20260222-1712）提到碰撞時放棄的版本中有「趨勢方向判斷」功能（判定市場趨勢為增加/減少/持平），A44T 保留的版本沒有這個功能。該功能對標案選案有實際價值——知道某領域的案件量是在成長還是縮減，影響是否投入。
操作:
1. types.ts — MarketTrend 介面新增 trendDirection 欄位（"增加" | "持平" | "減少"）
2. market-trend.ts — 新增 judgeTrend 純函式：比較前半段和後半段年度的平均案件量，差異超過 20% 才判定方向變化
3. market-trend.ts — analyzeMarketTrend 呼叫 judgeTrend，空資料預設持平
4. MarketTrend.tsx — 總覽區的「涵蓋年度」卡片改為「趨勢方向」卡片（顯示方向箭頭 + 文字 + 年數）
5. market-trend.test.ts — 新增 7 個 judgeTrend 測試（不足2年、增加、減少、持平、前半為0、都是0、剛好2年）
結果: 36 檔 749 測試全過（market-trend 10→17），build 零錯誤。
---
OP|20260222-2005|ITEJ|成功|topic:cleanup-type-assert
消除 knowledge-base/helpers.ts 雙重型別斷言 + 論壇回覆 P0 安全 directive|F:src/lib/knowledge-base/helpers.ts,forum/20260222-ITEJ.md
---
背景: 前次 session 的程式碼品質掃描發現 searchEntries 函式中有 `entry as unknown as Record<string, unknown>` 雙重型別斷言，是因為 extractSearchableText 參數型別定義為 Record<string, unknown> 但呼叫端傳入的是泛型 T。同時 pull 帶進了 JDNE 的 P0 安全 directive（資訊安全最高優先級）和 AINL 的機器-用戶對應 brief。
操作: 將 extractSearchableText 參數從 Record<string, unknown> 改為 object，消除呼叫端雙重斷言。tsc 零錯誤、40 檔 865 測試全過。回覆論壇確認收到 P0 directive 和 AINL brief。
結果: 型別斷言清理完成，安全規則確認。
---
OP|20260222-2010|ITEJ|成功|topic:feat-pcc-web
useFeatureEnabled hook + FeatureGuard 元件測試（+17 tests）|F:src/lib/modules/__tests__/useFeatureEnabled.test.ts,src/lib/modules/__tests__/FeatureGuard.test.tsx
---
背景: 程式碼品質掃描發現 modules/ 目錄中 useFeatureEnabled.ts（2 個 hook）和 FeatureGuard.tsx（路由守衛元件）完全沒有測試。feature-registry.ts 的純函式已有 35 個測試，但依賴 React context 的部分未覆蓋。
操作:
1. useFeatureEnabled.test.ts — 9 個測試：預設啟用、未知 ID、顯式停用/啟用、settings 變更即時反映；useEnabledFeatures 返回全部/排除停用/排除多個/物件形狀
2. FeatureGuard.test.tsx — 8 個測試：啟用路由渲染子元件、非管控路由通過、停用顯示關閉訊息、關閉訊息含設定連結、顯示功能名稱、深層子路由匹配/攔截、多功能啟用
3. mock 了 next/navigation（usePathname）和 next/link
結果: 42 檔 882 測試全過（865→882，+17），tsc 零錯誤。
---
OP|20260222-2130|ITEJ|成功|topic:feat-test-coverage
知識庫 CRUD hook + 選項管理 hook + logger hook 測試（+49 tests）|F:src/lib/knowledge-base/__tests__/useKnowledgeBase.test.ts,src/lib/knowledge-base/__tests__/useCustomOptions.test.ts,src/lib/logger/__tests__/useLogger.test.ts
---
背景: 論壇巡邏的「找事做」階段，掃描全 codebase 未測試模組。發現 knowledge-base/ 的 useKnowledgeBase（CRUD 操作）和 useCustomOptions（選項管理）兩個 hook 完全未測試，logger/ 的 useLogger hook 也未測試。底層純函式已有測試，但 React hook 整合層未覆蓋。
操作:
1. useKnowledgeBase.test.ts（22 tests）：初始化（空/載入/損壞/部分資料/hydration/API 暴露）、新增（00A/00E/累積）、更新（ID 更新/保留其他欄位/不影響其他/不存在 ID）、刪除（刪除/不存在/不跨 KB）、狀態更新（archived/draft）、匯入匯出（部分/覆蓋/匯出）、清空、localStorage 持久化
2. useCustomOptions.test.ts（17 tests）：初始化、getOptions（預設/所有 key/覆蓋）、addOption（新增/去重/trim/空值）、removeOption（移除/不存在）、resetOptions（重設/不影響其他）、localStorage 持久化（儲存/載入/損壞）
3. useLogger.test.ts（10 tests）：初始化、log（新增/details+source/累積最新優先）、clear、exportJson（有資料/空）、過濾（level/category）
結果: 46 檔 947 測試全過（897→947，+49 新測試，扣 AINL 的 1 個 docgen test），build 零錯誤。
---
OP|20260222-2245|ITEJ|成功|topic:cleanup-console-log
客戶端 console.warn/error 統一換成 logger（4 檔）|F:useCustomOptions.ts,useKnowledgeBase.ts,FieldMappingEditor.tsx,page.tsx
---
背景: 論壇巡邏的「找事做」階段，對全 codebase 做程式碼品質掃描。發現 4 處客戶端程式碼使用 console.warn/error 而非 logger 系統，違反 CLAUDE.md 日誌規範（第 9 條：關鍵操作必須透過 logger 記錄）。
操作:
1. useCustomOptions.ts — console.warn → logger.warn("system", ...)
2. useKnowledgeBase.ts — console.warn → logger.warn("system", ...)
3. FieldMappingEditor.tsx — console.error → logger.error("api", ...)
4. page.tsx — console.warn → logger.warn("api", ...)
後三檔需新增 logger import，page.tsx 已有。API routes（pcc、smugmug、notion）的 server-side console.error 判斷為合理保留。
結果: tsc 零錯誤，47 檔 953 測試全過（A44T 的 notion/client +5 和 KB version upgrade +1），build 零錯誤。客戶端 console 殘留為零。
---
OP|20260222-2400|ITEJ|成功|topic:feat-test-coverage
00B 知識庫煙霧測試 + 論壇回覆補回 + docgen 跨機器審查|F:useKnowledgeBase.test.ts,forum/20260222-ITEJ.md
---
背景: 論壇巡邏繼續找事做。AINL 審查 ITEJ 測試時建議補 00B（實績案例）煙霧測試。另外多次 force push 導致先前寫的論壇回覆丟失（JDNE P0/P1 directive 確認、AINL 審查回饋、A44T docgen 跨機器審查），需補回。
操作:
1. useKnowledgeBase.test.ts — 新增 makeEntry00B factory 函式 + 1 個煙霧測試（add→update→delete 全週期）
2. 補回丟失的論壇回覆：JDNE P0 安全清理 ×4 確認、P1 幕僚監控/無用戶規則/稽核結果/工作分配確認、暱稱/經驗廣播回覆
3. A44T docgen 模組跨機器審查（feedback）：確認 margins * 10 是正確的 cm→mm 轉換，建議加註解和檔名衛生
結果: tsc 零錯誤，47 檔 954 測試全過（+1 00B test），build 零錯誤。
---
OP|20260223-2140|ITEJ|成功|topic:feat-test-coverage
PCC hook 邊界測試 +8：committee MAX_DETAILS/空結果快取/成功快取/非Error、market 空白keyword/分頁中斷、agency 無companies/50筆上限/unmount取消|F:useCommitteeAnalysis.test.ts,useMarketTrend.test.ts,useAgencyIntel.test.ts
---
背景: 繼續補測試覆蓋。掃描 PCC 5 個 hook 的測試，發現 useCommitteeAnalysis、useMarketTrend、useAgencyIntel 有邊界未覆蓋。usePCCSearch 和 useCompetitorAnalysis 已完整。之後掃描全 src/lib/ 確認所有含邏輯的模組都有測試。
操作:
1. useCommitteeAnalysis.test.ts +3：MAX_DETAILS(30) 上限（35 筆只取 30）、空結果不快取（cacheSet 未呼叫）、成功快取驗證、非 Error throw
2. useMarketTrend.test.ts +2：純空白 keyword 跳過、分頁途中失敗（page 2 timeout 不快取）
3. useAgencyIntel.test.ts +3：無 companies 欄位記錄跳過、awards.slice(0,50) 上限、unmount 後取消（cancelled flag）
4. 全 src/lib/ 掃描確認：僅 chart-config.ts（re-export）和 seed-data.ts（純資料）無測試，合理免測
結果: 54 檔 1050 測試全過（committee 10→13, market 9→12, agency 13→16），全 lib/ 模組測試覆蓋完成。
---
OP|20260222-2240|ITEJ|成功|topic:feat-test-coverage
seed-data 結構驗證 +15 tests + sanitizeFilename +7 tests + 文件生成頁面章節排序|F:seed-data.test.ts,generate-docx.ts,page.tsx(docx)
---
背景: 延續測試覆蓋工作。seed-data.ts 是純資料但有結構約束（ID 格式、必填欄位、步驟順序），值得驗證。docx 生成的 downloadBlob 使用檔名直接來自用戶輸入，需要衛生處理。文件生成頁面描述說「章節可調整順序」但沒有 UI。
操作:
1. seed-data.test.ts +15：SEED_00C（6）+ SEED_00D（7）+ 跨 seed 一致性（1）+ 風險等級覆蓋（1）。驗證 ID 格式（T-/R- prefix）、必填欄位、phases ≥3、response steps ≥2 且順序正確
2. generate-docx.ts：新增 sanitizeFilename 函式（替換 /\\:*?"<>| 為 _，空值返回 "untitled"），downloadBlob 改用 sanitizeFilename
3. generate-docx.test.ts +7：sanitizeFilename 7 個邊界（中文、空字串、whitespace-only、dots/hyphens 保留）
4. docx/page.tsx：加 moveChapter 函式 + 上移/下移按鈕（disabled 在頂/底時）
結果: 55 檔 1132 tests 全過，tsc 零錯誤。章節排序 UI 完成。
---
OP|20260222-2330|ITEJ|成功|topic:plan-m03-strategy
M03 戰略分析引擎規格草案 v0.1|F:bidding-assistant/docs/dev-plan/M03-戰略分析引擎.md
---
背景: 用戶指定 ITEJ 角色轉為策略開發人員，負責規劃模組功能規格讓開發機器接手。4 個模組待寫（M02/M03/M04/M06），判斷 M03 最優先：它是情報（M01）到決策的橋樑，M01 已 v1.1 定案且完整實作，但缺少把數據變成投標判斷的「大腦」。
操作:
1. 盤點現有基礎設施：讀取 lib/pcc/ 全部分析模組（analysis.ts、market-trend.ts、committee-analysis.ts、scout-report.ts）、lib/dashboard/useCrossAnalysis.ts、lib/knowledge-base/types.ts、data/config/stages.ts
2. 讀取 00-總論（6 大能力定義）、05-工作流程（PCC→P偵察→L1 流程）、M01 完整規格（資料流和介面）
3. 設計 M03 四大功能：適配度評分（5 維 0-100）、投標決策報告、作戰計畫、投標組合分析
4. 產出完整規格草案（含型別定義、演算法邏輯、UI 設計、資料流、4 期分期、5 個設計決策）
5. 更新 01-索引（v1.3→v1.4，M03 從「待寫」改「v0.1 草案」）
決策: 適配度評分用確定性演算法不用 AI（可解釋+可測試+$0）；KB 匹配用關鍵字不用向量（<200 筆規模夠用）；M03 不取代 L1 而是預組裝硬數據讓 L1 更準
檔案: bidding-assistant/docs/dev-plan/M03-戰略分析引擎.md（新建 v0.1）、01-索引.md（v1.4）
結果: M03 規格草案完成，詳細到開發機器可直接從 Phase 1 開始實作。四期分期各有明確驗收標準。
---
OP|20260222-2345|ITEJ|成功|topic:plan-m04-quality-gate
M04 品質閘門模組規格草案 v0.1|F:bidding-assistant/docs/dev-plan/M04-品質閘門模組.md
---
背景: 繼 M03 後，接續第二優先的模組規格。現有品質模組（lib/quality/）只做文字表面檢查（禁用詞、鐵律、重複句等 13 條規則），缺乏深層語義檢查——AI 產出可能包含幻覺、未回應招標需求、提出不切實際的方案。
操作:
1. 讀取現有 lib/quality/ 全部檔案（types.ts、rules.ts、constants.ts、score.ts），確認 13 條規則和 55 個測試的完整結構
2. 設計三道新閘門：事實查核（知識庫來源追溯+幻覺偵測）、需求對照（追溯矩陣+覆蓋率）、實務檢驗（預算/時程/團隊/常識）
3. 決定新建 lib/quality-gate/ 而不是擴展 lib/quality/（關注點不同+依賴不同+不破壞現有測試）
4. 產出完整規格（型別定義、與 M03 的關係、品質報告結構、UI 設計、4 期分期）
5. 更新 01-索引（v1.5，M04 從待寫改為 v0.1 草案）
決策: 新建 quality-gate/ 不擴展 quality/；閘門 1-3 優先用確定性規則（regex+比對+計算）而非 AI；閘門 2 允許無需求清單時跳過（有多少資料做多少檢查）
檔案: bidding-assistant/docs/dev-plan/M04-品質閘門模組.md（v0.1）、01-索引.md（v1.5）
結果: M04 規格草案完成。三道閘門設計清楚，與現有品質模組的邊界明確，開發機器可從 Phase 1（事實查核）開始。
