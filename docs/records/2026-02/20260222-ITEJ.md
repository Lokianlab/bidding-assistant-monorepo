OP|20260222-1720|ITEJ|成功|topic:feat-pricing-refactor
報價計算模組重構：邏輯抽出到 lib/pricing/、26 個測試|F:src/lib/pricing/types.ts,src/lib/pricing/helpers.ts,src/app/tools/pricing/page.tsx
---
背景: 報價計算模組（pricing/page.tsx）所有邏輯都內嵌在頁面元件中，無測試，與品質檢查模組重構前的狀態相同。依照專案架構規範，應該把商業邏輯抽到 lib/ 層。
操作:
1. 建立 src/lib/pricing/types.ts — CostItem、CostCategory、PricingConfig、PricingSummary 型別
2. 建立 src/lib/pricing/helpers.ts — itemAmount、calcCategoryTotals、calculateSummary、formatAmount、createEmptyItem、getDefaultItems 六個純函式
3. 建立 src/lib/pricing/__tests__/helpers.test.ts — 26 個測試覆蓋所有計算邏輯（單筆計算、類別小計、完整摘要含邊界值、工具函式）
4. 重構 page.tsx — 從 255 行內嵌邏輯改為匯入 lib 模組的薄包裝層，移除 CostItem 和 EMPTY_ITEM 定義
結果: 33 檔 681 測試全過（新增 26 個），build 零錯誤。頁面行為完全不變，邏輯可獨立測試。
---
OP|20260222-1730|ITEJ|成功|topic:feat-assembly-refactor
提案組裝引擎重構：純邏輯抽出到 lib/assembly/、31 個測試|F:src/lib/assembly/types.ts,src/lib/assembly/helpers.ts,src/app/assembly/page.tsx
---
背景: 提案組裝引擎（assembly/page.tsx）是產品核心功能，724 行中混雜了純計算邏輯和 React 狀態管理。token 估算、檔案清單計算、檔案名稱生成等純函式應該抽到 lib 層以便獨立測試。
操作:
1. 建立 src/lib/assembly/types.ts — FileListItem 介面
2. 建立 src/lib/assembly/helpers.ts — estimateTokens、formatKB、buildFilename、computeFileList、computeActiveFiles、assembleContent 六個純函式
3. 建立 src/lib/assembly/__tests__/helpers.test.ts — 31 個測試（token 估算 5 + 格式化 4 + 檔名 6 + 檔案清單 6 + 啟用清單 5 + 組裝 4 + L3 撰寫規範驗證）
4. 重構 page.tsx — 刪除內嵌函式，改為 import lib 模組；fileList 和 activeFiles 的 useMemo 改為呼叫 computeFileList / computeActiveFiles；handleAssemble 改用 assembleContent
結果: 35 檔 723 測試全過（+42 新測試），build 零錯誤。頁面行為不變，核心計算邏輯可獨立測試。
---
