OP|20260223-0401|JIVK|topic:infra-operating-constraints
首次啟動。Jin 指示新增 Agent 運作約束 v1.0（46 條），存入 .claude/rules/operating-constraints.md。
同時建立 docs/optimizations.md（F-3 條文要求的跨 agent 優化知識沉澱）。
與現有 CLAUDE.md 互補不取代，衝突時以 CLAUDE.md 為準。
---
OP|20260223-0417|JIVK|topic:infra-claude-md-split
Jin 提供手動拆分的 CLAUDE.md，比現有單檔架構更優（247→96 行，-61%）。
執行拆分：CLAUDE.md 瘦身 + 4 個 .claude/rules/ 新檔（sync-protocol / record-layer / governance / project-context）。
修正：sync-protocol.md 重啟流程的「讀 rules」步驟改為「系統自動載入」（.claude/rules/ 本來就是自動載入的）。
附帶整理：operating-constraints.md 移到 docs/methodology/（避免重複載入，新 CLAUDE.md 已吸收重點）。
optimizations.md 移到 docs/methodology/（對齊 CLAUDE.md 引用路徑）。
新增規則：所有時間戳的日期部分必須使用 currentDate（Jin 指示）。
---
