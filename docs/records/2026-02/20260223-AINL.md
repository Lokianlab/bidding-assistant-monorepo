OP|20260223-0110|AINL|成功|topic:feat-test-coverage
補 quality/constants SSOT 驗證測試 +5 tests|F:bidding-assistant/src/lib/quality/__tests__/constants.test.ts
---
背景: quality/constants.ts 是 RULE_NAMES 和 IRON_LAW_LABELS 的 SSOT，被 rules.ts 和 page.tsx 共用，但沒有任何測試。任何改動都可能悄悄破壞映射關係。
操作: 建 quality/__tests__/constants.test.ts，5 個測試：RULE_NAMES 包含所有鍵、值非空字串、值無重複、IRON_LAW_LABELS 五個鐵律 flag 存在且長度正確、每個 label 都在 RULE_NAMES 中。
結果: 5 tests 全過，62 檔全套通過。
---
OP|20260223-0100|AINL|成功|topic:feat-test-coverage
補 useFitScore hook 測試覆蓋 +7 tests|F:bidding-assistant/src/lib/strategy/__tests__/useFitScore.test.ts
---
背景: strategy 模組三個純函式（fit-scoring/kb-matcher/helpers）有測試，但 useFitScore hook 沒有任何測試覆蓋。hook 是純函式的橋接層，主要邏輯包含：空案名 guard、settings 讀取（fitWeights fallback）、memoization。
操作: 建 useFitScore.test.ts，6 個測試案例：空案名/純空白案名回傳 null、有效案名回傳 FitScore 物件、回傳 KBMatchResult、五個維度存在、verdict 有效值、預算 null 不崩潰。使用 SettingsProvider wrapper（同 useCardLayout 測試慣例）。
結果: 7 tests 全過，61 檔 1284 tests 全過。
---
OP|20260223-0040|AINL|成功|topic:infra-methodology-v2
修正 CLAUDE.md 待決事項規則：畫面分兩部分+同時並行缺一不可|F:CLAUDE.md
---
背景: 上版把「畫面分兩部分」改成「同時並行」，但用戶指出兩者都是重點——畫面分兩部分確保用戶不在電腦旁時回來後第一眼就看到待決事項；同時並行確保 AI 不停下來等。
操作: 修正 CLAUDE.md 第 210 行，改為「輸出分兩個部分：上方顯眼區塊列待決事項（用戶不在電腦旁時的保障）；同時在下方展開繼續工作——兩件事必須同時發生」。
結果: 語義完整，兩個設計原因都保留。
---
brief|20260223-0025|AINL|ref:none
feat-scout-committee 完整打通（含 UI 接線修正）。
變動：(1) scout-report.ts 加入評委情報段——外聘/機關人數、外聘委員名單含經歷（上限 5 人）、「評委背景」查詢項；buildScoutInput 同步擴充。(2) PCCTenderSheet.tsx 修正 scope bug：ScoutReportButton 子元件直接引用父元件 detail 狀態（TS2304），改為透過 committee prop 傳入。
影響：PCCTenderSheet.tsx 和 scout-report.ts 都有改動。有在改這兩個檔案的機器，rebase 時注意衝突。
---
OP|20260223-0020|AINL|修正|topic:feat-scout-committee
修正 P偵察 UI 接線：ScoutReportButton 加入 committee prop|F:bidding-assistant/src/components/pcc/PCCTenderSheet.tsx
---
背景: 上一個 session 將評委情報加入 buildScoutInput，但 UI 接線有型別作用域錯誤——PCCTenderSheet 的 detail 狀態在 ScoutReportButton 子元件中不可見，導致 tsc error TS2304: Cannot find name 'detail'。
操作: 修正 ScoutReportButton：(1) props interface 新增 committee?: EvaluationCommitteeMember[] (2) handleCopy 改用 committee prop 取代 detail?.evaluation_committee (3) useCallback deps 補入 committee (4) 呼叫端 <ScoutReportButton> 傳入 committee={detail?.evaluation_committee}。
結果: tsc 零錯誤，57 檔 1195 tests 全過。評委情報現在從 detail.evaluation_committee 正確傳入 Scout Prompt。
---
