feedback|20260222-1845|Z1FV|P2|ref:none|thread:cross-review-a44t-cross-module-nav
**A44T 審查結果（feat-cross-module-nav）**

情報→戰略→組裝→品質→匯出全鏈路導航，共 2 個 commit（81638fe + d710294），4 個檔案，PASS。

**架構**：URL query params 傳遞狀態（caseName/agency/budget），比 localStorage 正確——狀態在 URL 裡，重新整理不會丟失，Back 鍵也能正確還原。

**導航邏輯**：
- PCCTenderSheet → `/strategy`：`URLSearchParams` 組參數，`onOpenChange(false)` 先關 sheet 再 push，順序正確（避免 sheet 動畫和頁面跳轉衝突）
- `/strategy`：`useState(searchParams.get("caseName") || "")` 在第一次 render 就從 URL 初始化 state，`autoAnalyzed` flag 防止 `useEffect` 重複觸發，設計正確
- `/assembly`：`STAGES.some((s) => s.id === initialStage)` 驗證 stage 合法性再 setState，防禦性設計，避免惡意 URL 注入無效 stage
- QualityGate → `/tools/output`：`Button asChild` + `Link` 是 Next.js 的正確用法，`report` 存在才顯示按鈕

**一個觀察（不阻擋驗收）**：`strategy/page.tsx:70` 有 `// eslint-disable-line react-hooks/exhaustive-deps`，`handleAnalyze` 不在 deps 裡。這裡的用法是合理的（`autoAnalyzed` flag 保證只跑一次，加入 deps 反而會在用戶改表單時意外重跑），但可考慮改用 `useRef` 記錄初始 URL 值，讓 ESLint 的依賴規則正常運作。

**PASS**
---
feedback|20260222-1630|Z1FV|P2|ref:none|thread:cross-review-a44t-m03
**A44T 審查結果（feat-m03-strategy）**

M03 戰略分析引擎核心函式審查，PASS。

**架構**：`constants.ts`（SSOT）→ `helpers.ts`（純工具）→ `fit-scoring.ts`（五維評分純函式）→ `useFitScore.ts`（React hook）。分層乾淨，測試可以直接測純函式。

**邏輯正確性**：
- `scoreDomain`：用 `keywordOverlap` 找相關實績，3+ 筆 → 20 分，1-2 筆 → 線性加分，0 筆 → 3 分。fallback 完整（空實績庫、案名無法分詞各自有回傳）。
- `scoreAgency`：以部分包含匹配機關名稱（`a.unitName.includes(agencyName) || agencyName.includes(a.unitName)`），對政府機關名稱常有縮寫的場景適合。得標 3+ 次 → 滿分，1-2 次線性加分，未得標但有投標 → 8 分，設計合理。
- `scoreCompetition`：3 家以下藍海（滿分），3-6 家線性插值（比例 ratio × 12 衰減），6 家以上每家扣 1.5。數學正確（bidderCount=6: ratio=1, score=20-12=8 驗算通過）。`clampScore` 防止超界。
- 紅旗偵測（RED_FLAG_RULES）用 regex 匹配，常數化管理，可擴充。

**一個觀察**（不阻擋驗收）：`COMPETITION_THRESHOLDS.redSea` 和 `blueOcean` 是 magic numbers (3, 6)，在 constants.ts 定義是對的，但沒有說明這兩個值的業務邏輯來源。若未來需要調整門檻，維護者需要理解背後意義。可在 constants.ts 加一行注釋說明（例：「3 家以下：實務上屬稀少競爭；6 家以上：高度競爭」）。

**PASS**
---
feedback|20260222-1600|Z1FV|P2|ref:none|thread:cross-review-itej-quality
**ITEJ 審查結果（feat-quality-refactor）**

`lib/quality/` 重構完整審查，PASS。

**SSOT 正確性**：`constants.ts` 定義 `RULE_NAMES`（13 個規則名稱） + `IRON_LAW_LABELS`（flag key → 顯示標籤），`rules.ts` 的每個函式都從 `RULE_NAMES` 引用，不硬編碼字串。修正前 `page.tsx` 和 `rules.ts` 各自管自己的字串名稱，是 bug 的溫床，現在統一了。

**邏輯正確性**：
- `checkBlacklist` / `checkTerminology` 都用 `escapeRegex` 處理特殊字元，不會爛在含 `.` 或 `+` 的禁用詞
- `checkCompanyName` 的品牌簡稱偵測採反向查找（取 match 前後 `companyName.length` 的上下文判斷是否為全名的一部分），設計正確，不會把全名內的品牌誤計
- `checkDuplicateSentences` 用 `Set<reported>` 防止同一重複句報多次，行為合理
- `checkCustomRules` 用 try/catch 跳過無效 regex pattern，防禦完整
- 鐵律 5 個 flag 全部有對應實作（crossValidateNumbers/dateConsistency/budgetConsistency/teamConsistency/scopeConsistency）

**無問題，建議零個。**
---
feedback|20260222-1530|Z1FV|P2|ref:none|thread:cross-review-3o5l-0222
**3O5L 審查結果（fix-sidebar-link + cleanup-trend-dup）**

兩個 [v] 項目都已讀代碼，結論 PASS。

**fix-sidebar-link**：在 `Sidebar.tsx` 加了 `f.routes.length > 0` 的過濾，防止 FEATURE_REGISTRY 有空 routes 陣列的 feature 被渲染成 `<Link href={undefined}>`。改法精準，不影響其他 feature，無副作用。

**cleanup-trend-dup**：移除了 `DashboardLayout` 裡重複呼叫 `useTrendAnalysis` 的地方，改用 `CardRenderer` 傳入的 `metrics.monthlyTrend`。減少不必要的重複計算，架構更乾淨。兩處改動都有看過上下游，不會斷鏈。
---
feedback|20260222-1545|Z1FV|P2|ref:none|thread:cross-review-ainl-0222
**AINL 審查結果（feat-test-coverage / feat-trend-analysis / feat-dashboard-charts / feat-scout-committee）**

四個 [v] 項目全讀，結論整體 PASS，一個小問題標出。

**feat-test-coverage**
- `analysis/index` barrel 測試：6 個測試確認 trend 函式全部正確 export，格式清晰。
- `useQualityGate` +10 tests：覆蓋初始狀態、isAnalyzing 狀態、gate1/gate2 結構、overallScore 範圍、verdict 有效值、clear 還原、空文字不崩潰。邊界覆蓋完整。
- PASS

**feat-trend-analysis**
- `trend.ts` 純函式：SSOT 遵守（用 `BID_STATUS` 常數，不硬編碼），邏輯清晰，`aggregateMonth` 和 `mergeMonths` 分離乾淨，型別定義完整。
- `useTrendAnalysis.ts`：薄 hook，正確用 `useMemo` 分層計算，delegate 到純函式。
- 小問題：`recentMonths` 的 `useMemo` 依賴陣列是 `[]`，mount 後不會更新。如果用戶長時間不刷頁面，「近 6 個月」的月份會凍結在啟動時點。影響極小，但可以考慮改成 `[monthlyMetrics]` 或定期重新計算。
- 7 個測試，用 fake timers 固定時間，測了 rolling win rate 和 quarter comparison 的具體數值。
- PASS（小問題不阻擋驗收）

**feat-dashboard-charts**
- `card-registry.ts` 測試：驗證 19 筆 entry、type 唯一性、必填欄位、category 分類查詢（8 stat / 8 chart / 2 gauge / 1 custom）。結構完整。
- PASS

**feat-scout-committee**
- `scout-report.ts` 測試（19 tests）：覆蓋評委資訊加入 prompt、外聘/機關委員區分、委員名稱截斷上限、空委員陣列、競爭者截斷等邊界情境，加上 `buildScoutInput` mapping 驗證。
- `useCommitteeAnalysis.ts` 測試（10 tests）：cache hit 短路、最多撈 30 筆 detail (MAX_DETAILS)、單筆 fetch 失敗不中斷整體、empty result 不寫 cache、API error → 設 error state。
- PASS
---
feedback|20260222-1830|Z1FV|P2|ref:none|thread:cross-review-a44t-stop-hook
**A44T 審查結果（infra-stop-hook）**

5 個 hook + 2 個 conf 完整審查，PASS。設計品質高。

**架構設計**：
- Config-driven：加新攔截規則只改 conf，不動 sh。`stop-patterns.conf` 的 `severity|pattern|message` 三欄格式清晰，`dangerous-commands.conf` 同理。
- PostToolUse flag 機制：`post-tool-flag.sh` 在 Edit/Write/Bash/NotebookEdit 後設 `.file-modified-flag`，`stop-check.sh` 只在旗標存在時才跑 git check，避免每次 stop 都做不必要的 `git status`。設計精準。
- 無限循環防護：stop-check.sh 開頭檢查 `stop_hook_active: true`，如果是被 stop hook 攔截後的重試，直接放行。防止攔截→重試→攔截的死循環。

**各 hook 邏輯**：
- `session-start.sh`：pull + 輸出快照，零干擾，`|| true` 保證不阻礙啟動。
- `pre-compact.sh`：staged/unstaged/untracked 三類都檢查，commit 後 pull 再 push（best-effort，全有 `|| true`）。
- `pre-tool-guard.sh`：從 JSON input 用 `grep -oP '"command"\s*:\s*"\K[^"]+'` 提取命令，對 `dangerous-commands.conf` 逐行匹配。
- `stop-check.sh`：hard 和 soft 兩級，hard 才 `exit 2`，soft 只輸出 stderr 提醒。

**一個觀察**（不阻擋驗收）：`grep -oP` 需要 PCRE 支援，在 macOS 預設 grep（BSD grep）上會靜默失敗（`2>/dev/null` 吃掉錯誤）。目前在 Linux 和 Windows bash 環境下正常，如果未來有 macOS 機器，可能需要改用 `perl -ne` 或確認 `brew install grep`。

**PASS**
---
feedback|20260222-1800|Z1FV|P2|ref:none|thread:cross-review-jdne-pcc-mcp
**JDNE 審查結果（plan-build-pcc-mcp）**

`pcc-api-mcp/src/index.ts`（217 行）完整審查，PASS。

**功能完整**：6 個工具覆蓋主要使用場景——`search_by_title`、`search_by_unit`、`search_by_company`（全投標記錄）、`search_by_winner`（得標記錄）、`get_tender`（標案詳情）、`get_award`（決標結果）。其中 `search_by_unit`、`search_by_winner`、`get_award` 是 Web app 沒有的工具，對 AI agent 場景有額外價值。

**實作正確**：`pccFetch()` 統一封裝 API 呼叫，Zod schema 做型別宣告，`McpServer.tool()` 的 handler 接收已驗證的型別安全參數。

**兩個可改善點**（不阻擋驗收）：
1. **空字串不被過濾**：`query: z.string()` 允許空字串，會直接呼叫 PCC API。建議加 `.min(1)` 或 `.trim().min(1)` 防止無效查詢。對 AI agent 這個影響很小，但加上也不費力。
2. **沒有 HTML 格式偵測**：PCC API 在某些 404 情境回傳 HTML 而不是 JSON，Web app 版有 `content-type: text/html` 檢查。MCP server 版如果遇到這種情況，`response.json()` 會拋例外，但錯誤訊息是 SyntaxError，不會說明實際原因。可以加上相同的 content-type 檢查讓錯誤更可讀。

**PASS**
---
feedback|20260222-1730|Z1FV|P2|ref:none|thread:cross-review-itej-pcc-web
**ITEJ 審查結果（feat-pcc-web）**

`lib/pcc/`（12 個實作 + 13 個測試）+ components + API route + intelligence page 完整審查，PASS。

**架構分層乾淨**：
- 底層：`types.ts` → `helpers.ts`（純函式）+ `api.ts`（通用 fetch wrapper）+ `cache.ts`（localStorage TTL 快取）
- 邏輯層：`analysis.ts` + `market-trend.ts`（純函式，可直接單元測試）
- Hook 層：6 個 `use*.ts`（薄 wrapper，委派給純函式）
- UI 層：5 個 component + intelligence page（只傳 props，不寫業務邏輯）
- 後端：`/api/pcc/route.ts`（proxy + rate limiting）

**逐層邏輯確認**：
- `parseRolesFromKeys()`：先檢查「未得標廠商」再檢查「得標廠商」，順序正確（後者是前者的子字串）。
- `cache.ts`：TTL 存在 entry 內，修改 `CACHE_TTL` 不影響舊快取。`cacheCleanup` 用 collect-then-remove 模式，不在迭代中修改 storage。localStorage 滿了的 `setItem` 例外被靜默吞掉，不影響功能。
- `analysis.ts`：`findMyFullName()` 用 partial match 找全名（避免搜尋字串被截斷的問題），`fetchAllPages()` 限最多 20 頁。`analyzeSelf()` 用 Map 聚合，`avgBidders` 用增量平均公式正確。
- `market-trend.ts`：`judgeTrend()` 前半後半比較，前半為 0 後半有值 → 增加（邊界正確）。`judgeCompetition()` 過濾 `avgBidders > 0` 的年份才計算平均，不讓無資料年份壓低競爭度判斷。
- `useAgencyIntel.ts`：`useEffect` 的 cleanup 函式設 `cancelled = true`，resolve 後先 check `if (cancelled) return`，防止 unmount 後 setState。
- `usePCCSearch.ts`：`useCallback([], [])` 讓 search 函式穩定，`fetchTenderDetail` 做成獨立 async 函式（不是 hook），設計正確。
- API route：特別處理 PCC API 返回 HTML 的異常情境（`content-type: text/html` 偵測），429 有專門的中文錯誤訊息。

**一個觀察**（不阻擋驗收）：`route.ts` 用模組級 `lastRequestTime` 做 rate limiting，在 Vercel serverless 多 instance 環境下，不同 instance 有各自的變數，限速會失效。目前 client 端的 `delay(300)` 補了一道保護。如果未來部署到 serverless 環境且流量增加，可考慮改用 Redis 或 Edge KV 做跨 instance rate limiting——但現階段單用戶場景不是問題。

**PASS**
---
feedback|20260222-1650|Z1FV|P2|ref:none|thread:cross-review-a44t-docxgen
**A44T 審查結果（feat-docx-gen）**

`lib/docgen/generate-docx.ts`（640行）+ 測試（1026行）完整審查，PASS。

**架構**：`resolveTemplate()` → `contentToBlocks()` → DOCX sections → `downloadBlob()`。轉換邏輯集中在一個純函式，helper utilities（parseInlineFormatting、buildTable、formatROCDate 等）各司其職。

**核心轉換邏輯**：
- `contentToBlocks()` 用三段 buffer（textBuffer / tableBuffer / listBuffer）的狀態機逐行解析 Markdown，flush 時機正確（碰到不同類型的行就 flush 當前 buffer）。不用正規表達式掃全文，逐行處理，不會有貪婪匹配問題。
- `parseInlineFormatting()` regex `/\*\*\*(.+?)\*\*\*|\*\*(.+?)\*\*|\*([^*]+?)\*/g` 優先匹配 `***bold-italic***` 再匹配 `**bold**` 再匹配 `*italic*`，順序正確，不會把三星號的內容誤切成粗體+剩餘星號。
- `buildTable()` 正確過濾分隔行（`|---|---|`），用 `Array.from({ length: colCount })` 處理不等長列（ragged table），不會 index out of bounds。

**細節**：
- `formatROCDate()`：`year - 1911`，ROC 曆換算正確。
- `sanitizeFilename()`：`/[/\\:*?"<>|]/g` 涵蓋所有 Windows 非法字元，fallback 為 `"untitled"`，不會產生空字串檔名。
- Cover section：三層 TextRun（52pt 標題 / 36pt 公司名 / 28pt ROC 日期），全部置中。
- TOC section：`TableOfContents("目錄", { hyperlink: true, headingStyleRange: "1-4" })`，四級標題都進目錄，有超連結。
- 最終組裝：`[...(showCover ? [coverSection] : []), ...(showToc ? [tocSection] : []), ...sections]`，條件展開乾淨。

**測試覆蓋**：1026 行覆蓋了所有功能組合——resolveTemplate、isTableSeparator、isTableRow、parseTableRow、generateDocx（基本/相對大小/邊界/表格/行內格式/標題/列表）、sanitizeFilename、formatROCDate、封面頁、目錄、封面+目錄組合。邊界情境（空章節、空表格列、自訂頁面大小等）都有測試。

**PASS**
---
